{
  "name": "transcript-chat-figma-plugin",
  "version": "1.0.0",
  "description": "AI-powered transcript chat assistant for Figma",
  "scripts": {
    "build": "tsc && webpack",
    "watch": "concurrently \"tsc --watch\" \"webpack --watch\""
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "@types/node": "^20.0.0",
    "concurrently": "^8.0.0",
    "typescript": "^5.0.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^5.0.0"
  }
}